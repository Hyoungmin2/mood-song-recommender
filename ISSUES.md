# Stage 2 추천 엔진 이슈 노트

기록일: 2026-08-21
업데이트: 2026-08-22 — 아래 이슈들 전부 수정 완료, 실행 확인함

## 진행 상황 요약 (다음에 이어서 볼 때 여기부터)

- **stage1**: K-means로 곡 10개 클러스터링하는 최소 동작 버전. 완료.
- **stage2**: Kaggle 데이터셋 기반 콘텐츠 추천(곡 기반 + 상황 텍스트 기반).
  아래 이슈들 다 수정 완료하고 여러 곡으로 재검증까지 함. 완료.
- **stage3 (진행 중)**: UI/제품화. `app.py`(Streamlit) 첫 버전 완성 —
  캐싱, 곡 검색(부분일치+fuzzy 폴백), 카드형 결과 UI까지 붙임. 실행/검증
  완료. 아직 안 한 것: Spotify 메타데이터(앨범아트/링크) 연동, 배포.
- **stage4 (진행 중)**: ML 요소 강화. 방향은 (1) 좋아요/스킵 피드백으로 학습되는
  재랭킹 모델(지도학습) (2) 상황 텍스트 파서 임베딩 기반 고도화 — **완료**
  (3) stage1 K-means를 취향 클러스터링으로 부활 (4) 정량 평가(precision@k
  등). (1) 피드백 수집 기능: 앱에 좋아요/스킵 버튼 붙여서 `feedback.csv`에
  기록하는 것까지 완료. (2) 임베딩 파서: 원격 개발 환경에서는 모델 다운로드가
  안 돼서(stage4-2b) 로컬에서 직접 설치/검증 진행. 시행착오 끝에
  (짧은 키워드 대신 예문 비교, 다국어 모델 대신 한국어 전용
  `jhgan/ko-sroberta-multitask`로 교체, threshold 실측) threshold 확정하고
  실제 Streamlit 앱에서 "장맛비라 꿀꿀하다" -> "장마" 의미 매칭 성공까지
  End-to-End 확인함 (stage4-2f). 다음 우선순위: (1) 재랭킹 모델(피드백 쌓인
  다음) / (3) K-means 부활 / (4) 정량 평가 / GitHub 업로드 중 결정 필요.
- **GitHub 업로드**: 완료 (2026-08-31). `git init` -> README.md 작성 ->
  `.env`/원본 데이터셋 CSV가 `.gitignore`로 잘 빠지는지 확인 -> 첫 커밋
  (`f4e5ac5`, 14개 파일) -> https://github.com/Hyoungmin2/mood-song-recommender
  로 push. `feedback.csv`는 의도적으로 커밋에 포함(프로젝트 자체 학습
  데이터라 계속 추적할 계획).
- **Streamlit Cloud 배포**: 완료 (2026-08-31). 1차 시도에서 `FileNotFoundError`
  발생(`.gitignore`의 `data/*.csv`가 앱이 실제로 읽는 가공 데이터셋까지
  같이 제외시킨 게 원인) - `!data/stage2_dataset.csv` 예외 규칙으로 수정
  (커밋 `3b845c6`). push 후 재배포 확인 결과 상황 텍스트 추천도 정상
  동작하고, 임베딩 기반 의미 매칭("의미유사" 문구)까지 클라우드에서
  정상 작동 확인함. stage4-2 + 배포 모두 End-to-End 완료.

## stage3: Streamlit 앱 (app.py) 첫 버전 (2026-08-22)

`recommend.py`/`context_parser.py`는 손대지 않고 그대로 재사용, `app.py`는
UI 레이어만 담당함.

- **캐싱**: `SongRecommender` 로딩(33,992곡 CSV 읽기 + `StandardScaler.
  fit_transform`)을 `st.cache_resource`로 앱 시작 시 한 번만 하도록 함.
  안 하면 상호작용마다 몇 초씩 걸려서 체감이 크게 나빠짐.
- **곡 검색(자동완성 대신)**: 정식 자동완성 위젯 대신, 입력한 문자열이 제목에
  부분적으로 포함되는 곡을 먼저 찾고(빠름), 없으면 fuzzy matching으로
  재시도해서 후보를 selectbox로 보여주는 2단계 방식으로 구현함. 후보가
  많으면 popularity 높은 순으로 정렬해서 상위 15개만 보여줌.
- **카드형 결과 UI**: 곡명/아티스트·장르/거리를 카드(`st.container(border=
  True)`)로 보여주고, 거리값을 `st.progress` 바로도 같이 표시함(가까울수록
  바가 꽉 차게, `1 - distance/3`으로 변환).
- **검증**: 컨테이너 안에서 streamlit 서버를 띄우고 Playwright로 직접
  클릭/입력/스크린샷까지 해서 확인함. bad guy 검색 결과, "비 오고 차분함" +
  Billie Eilish 결과(1위 고정 포함) 둘 다 CLI(main.py) 결과와 동일하게
  나오는 것 확인. 존재하지 않는 곡 검색 시 경고 문구도 정상 표시됨.
- **requirements.txt**에 `streamlit>=1.62.0` 추가함.

### 실행 방법

터미널에서 stage2 폴더로 이동한 뒤 `pip install -r requirements.txt`로
의존성 설치(스트림릿 처음이면 필요), 그다음 `streamlit run app.py` 실행하면
브라우저가 자동으로 열림.

### 다음 후보

Spotify 메타데이터(앨범아트/링크) 연동, 배포(Streamlit Community Cloud 등).

## stage4: "이 프로젝트에 진짜 ML이 있나?" 논의 및 방향 (2026-08-22)

### 문제의식

사용자가 지적: 지금 stage2/stage3의 실제 추천 흐름에는 학습된 모델이 없음.
`StandardScaler`+유클리드 거리 k-NN은 학습(파라미터 최적화) 없이 거리만
계산하는 방식이고, `context_parser.py`는 사람이 만든 규칙 사전, `MOOD_GENRES`
화이트리스트도 하드코딩. stage1에서 만든 K-means(진짜 비지도학습)조차 지금은
EDA용으로만 남아있고 실제 추천에는 안 쓰임.

### 결론 및 stage4 방향

"가수 기반 재랭킹"이 ML이냐는 질문에 대한 답: 규칙으로 가중치를 사람이
정하면 지금까지 해온 것들과 똑같은 휴리스틱이고 ML이 아님. 좋아요/스킵
피드백을 **학습 데이터**로 써서 분류 모델(로지스틱 회귀/랜덤포레스트 등)을
학습시키고, 그 예측값으로 재랭킹해야 진짜 지도학습이 됨. stage4는 아래
네 갈래로 진행하기로 함.

1. **재랭킹 모델(지도학습)** — 좋아요/스킵 데이터 → 분류 모델 학습 →
   예측 확률로 재랭킹. (핵심)
2. **임베딩 기반 텍스트 파서** — 규칙 사전을 사전학습 임베딩(예:
   sentence-transformers)으로 교체.
3. **stage1 K-means 부활** — 사용자가 좋아한 곡들을 클러스터링해서 취향
   프로필(여러 무드를 동시에 좋아함 등)을 뽑는 데 활용.
4. **정량 평가** — 좋아요 기록 일부를 테스트셋으로 떼어놓고 precision@k 등으로
   재랭킹 모델 성능 확인. "학습 → 평가"까지 갖춘 완결된 파이프라인으로.

1번은 3번 없이는 학습 데이터가 없어서 시작이 안 되므로, **피드백 수집
기능부터** 먼저 구현하기로 함(아래).

## stage4-1: 좋아요/스킵 피드백 수집 (2026-08-22)

`app.py` 결과 카드마다 👍 좋아요 / 👎 스킵 버튼을 추가하고, 클릭 시
`feedback.csv`(프로젝트 루트, `data/` 밖 — `.gitignore`의 `data/*.csv` 규칙에
안 걸리게 일부러 밖에 둠)에 timestamp/source(song 또는 mood)/context(검색한
곡·아티스트 또는 무드 문장)/추천된 곡 정보/label(like·skip)을 한 줄씩 기록함.

구현하면서 발견한 문제: Streamlit은 버튼 클릭 시 스크립트를 처음부터 다시
실행하는데, 추천 결과가 "추천받기" 버튼의 `if` 블록 안에서만 계산되고
있어서, 좋아요/스킵 버튼을 누르면(그 실행에선 "추천받기" 버튼이 안 눌렸으므로)
결과 카드 자체가 통째로 사라져버리는 문제가 있었음. `st.session_state`에
추천 결과를 저장해두고 항상 거기서 읽어서 렌더링하도록 구조를 바꿔서 해결함.

화면 상단에 "지금까지 모은 피드백: N개" 카운터도 추가해서 재랭킹 모델
학습에 필요한 데이터가 얼마나 쌓였는지 바로 보이게 함.

재검증: 곡 기반/상황 텍스트 기반 양쪽 다 좋아요·스킵 클릭 시 `feedback.csv`에
정상 기록되고, 클릭 후에도 카드가 안 사라지고 "🙏 좋아요 기록됨" / "🙏 스킵
기록됨" 표시가 뜨는 것까지 Playwright로 확인함.

### 다음 후보

피드백이 어느 정도 쌓이면 그 데이터로 재랭킹 모델(로지스틱 회귀 등) 학습
스크립트 작성. 그 전에 병행 가능한 건 임베딩 기반 텍스트 파서.

## stage4-2: 가벼운 사용자 구분(user_id) 추가 (2026-08-22, 사용자 질문에서 시작)

### 문제의식

사용자 질문: "재랭킹이 개인마다 다를 텐데 로그인 안 받아도 되냐?" — 맞는
지적. `feedback.csv`에 애초에 "누가" 눌렀는지가 없어서, 나중에 앱을
배포하면(stage3에서 예정) 다른 사람이 눌러본 좋아요/스킵까지 한 사용자의
취향 데이터에 섞여 들어가 재랭킹 모델이 "개인화"가 아니라 "여러 사람 취향이
뒤섞인 모델"이 될 위험이 있었음.

### 결정

정식 로그인(계정/비밀번호/DB)은 이 프로젝트 핵심(추천 ML)과 무관한 인프라라
과함으로 판단하고, 대신 닉네임만 입력받는 가벼운 방식을 택함 — 인증 없음
(비밀번호 없이 아무 이름이나 입력 가능), 그냥 `feedback.csv`에 `user_id`
컬럼을 추가해서 "누구 피드백인지"만 구분되게 함. 참고로 실제 서비스들도
신규 유저 몇 번 클릭만으로는 개인화 모델이 의미 있게 학습되지 않는
"콜드 스타트" 문제를 겪는 건 마찬가지라, 리크루터가 데모에서 몇 번 눌러본다고
그 사람만의 모델이 학습될 필요는 없음 — 이 정도 가벼운 구분으로 충분하다고
판단.

### 구현

`app.py` 사이드바에 닉네임 입력창 추가(`st.session_state["nickname"]`에
저장, 비어있으면 `DEFAULT_USER_ID = "guest"`로 대체). `FEEDBACK_FIELDS`에
`user_id`를 첫 컬럼으로 추가하고, `append_feedback()`/`render_result_cards()`
시그니처에 `user_id` 매개변수를 추가함. `count_feedback()`도 `user_id`를
주면 그 사람 것만 필터링해서 세도록 바꿔서, 사이드바에 "내 피드백: N개 /
전체 피드백: M개" 둘 다 보여줌.

재검증: 닉네임 "민"으로 입력 후 좋아요 클릭 → `feedback.csv`에 `user_id=민`
행으로 정상 기록, 사이드바에 "내 피드백: 1개 / 전체 피드백: 1개" 정상 표시
확인함 (Playwright).

### 다음 후보

피드백이 어느 정도 쌓이면 재랭킹 모델 학습(사용자 요청 시 `user_id`로
필터링해서 그 사람 데이터만으로 학습 가능).

## 설계 결정 메모 (버그는 아니지만 나중에 헷갈릴 수 있어서 기록)

- **인기도(popularity)는 필터로만 쓰고, 랭킹에는 반영 안 함.** `build_dataset.py`가
  popularity>=40인 곡만 데이터셋에 넣긴 하지만, 일단 후보로 들어온 다음부턴
  `recommend.py`가 쓰는 9개 특성에 popularity가 없어서 순위엔 전혀 영향 안 줌.
  (실측: `bad guy` 추천 3위였던 곡이 popularity 42로, 68짜리 1위 곡과 순위상
  동등하게 취급됨.) 나중에 "그래도 너무 무명곡만 나온다" 싶으면 popularity를
  랭킹에 반영하는 옵션을 다시 검토하기로 함 (거리값에 보정치로 섞기 / 최소
  popularity 기준 올리기 / 결과에 popularity 숫자만 같이 표시하기 중 택1).
- **Spotify Web API로 오디오 특성/추천 엔드포인트 접근은 여전히 불가.**
  2024년 11월 audio-features/recommendations 등 엔드포인트 신규 앱 차단,
  2026년 2월엔 Developer Mode 자체가 더 조여짐(개발자 Premium 계정 필수,
  Client ID당 사용자 5명 제한, 배치/브라우즈 엔드포인트 제거). search
  엔드포인트만 제한적으로(limit 최대 10) 살아있어서, `spotify_client.py`는
  메타데이터(앨범아트/링크) 보강용으로만 stage3에서 쓸 수 있음.

## 증상

`recommend.py`에서 곡 제목 기반 추천(`bad guy` / Billie Eilish)을 실행하면,
장르·분위기가 전혀 무관한 곡들이 유사도 0.94~0.96의 높은 점수로 추천됨.

```
Stay Alive (show-tunes)             유사도 0.959
Freestyle pra Faixa Rosa (funk)     유사도 0.958
Love Triangle (Pum Pum) (j-dance)   유사도 0.956
the overthinker (sad)               유사도 0.955
...
```

처음엔 "데이터셋(16,804곡, 113개 장르)이 작아서 그런가?" 라고 생각했지만,
실제 원인은 데이터 크기가 아니라 **유사도 계산 로직의 설계 문제**로 확인됨.

## 근본 원인

`recommend.py`는 9개 오디오 특성(danceability, energy, valence, tempo,
acousticness, instrumentalness, liveness, speechiness, loudness)을
`StandardScaler`로 평균 0 / 표준편차 1로 맞춘 뒤, 그 벡터에
**코사인 유사도(cosine similarity)**를 적용함.

문제: `StandardScaler`는 각 특성을 평균 중심(mean-centered)으로 이동시키는데,
코사인 유사도는 "원점에서 벡터가 가리키는 방향"을 비교하는 지표라서,
데이터가 원점(=평균) 근처에 몰려있는 상황에서는 **가장 튀는 특성 하나(이상치)가
전체 유사도 방향을 사실상 독차지**하는 현상이 생김.

### 실측 확인

`bad guy`(Billie Eilish)의 speechiness 값은 0.375로,
데이터셋 평균(0.087, 표준편차 0.11) 기준 z-score = **+2.63** (상위 약 1% 이상치).

추천된 상위 10곡의 speechiness를 확인한 결과 전부 0.39~0.63 사이로,
전부 "speechiness가 유난히 높은 곡" 그룹이었음. 즉 danceability/energy/valence는
사실상 무시되고, speechiness 이상치 하나만 보고 추천된 것.

### 대안 비교 (Euclidean distance로 교체 테스트)

같은 9개 특성에 코사인 대신 유클리드 거리를 쓰면 거리값이 줄어들긴 하지만
(0.95~1.4대), 여전히 speechiness 이상치 영향이 남아 있어 완전한 해결은 아님.

핵심 4개 특성(danceability, energy, valence, tempo)만 남기고 유클리드 거리로
계산하면 거리값이 **0.1~0.3대**로 확연히 좁혀짐 — 실제로 훨씬 더
비슷한 곡을 찾아줌. 다만 장르 태그 자체는 여전히 다양하게 섞여 나올 수 있음
(오디오 특성이 비슷해도 Spotify 장르 라벨은 다를 수 있어서 — 별개 이슈).

## 검토한 해결 옵션 (아직 미결정 — 사용자가 나중에 고를 예정)

### 옵션 1. 핵심 4개 특성(danceability/energy/valence/tempo) + 유클리드 거리

- 장점: 구현이 제일 간단함 (몇 줄만 수정). 실측 테스트에서 이미 효과 확인됨
  (거리값 0.95~1.4대 → 0.1~0.3대로 확 좁혀짐). 계산도 가벼워서 데이터가
  커져도 부담 없음.
- 단점: acousticness/speechiness/loudness 등 나머지 5개 특성을 아예 버리는
  거라 "음향적 질감"(어쿠스틱한지, 시끄러운지 등)은 반영 못함. 장르 정보도
  안 보기 때문에, 오디오 수치는 비슷해도 장르가 뒤섞여 나올 수 있음.

### 옵션 2. 9개 특성 전부 유지 + 코사인 → 유클리드 거리만 교체

- 장점: 코드 변경이 제일 적음 (거리 함수 한 줄만 교체). 기존에 공들여 넣은
  9개 특성을 그대로 활용.
- 단점: 실측해보니 이상치(speechiness 등) 영향이 완전히 사라지지 않음 —
  근본 원인(하나의 튀는 특성이 결과를 지배하는 문제)을 절반만 해결. 가장
  미봉책에 가까운 옵션.

### 옵션 3. `StandardScaler` → `RobustScaler`로 교체 (9개 특성 유지)

- 장점: 9개 특성을 하나도 버리지 않으면서 이상치 영향을 줄임 (중앙값/사분위
  기준으로 스케일링하기 때문). 나중에 특성별 가중치를 튜닝하고 싶을 때도
  특성 수가 많은 채로 남아있어서 유리함.
- 단점: 코사인 유사도를 계속 쓸 경우 "원점 근처에 몰린 벡터" 문제 자체는
  남아있어서, 유클리드 거리와 함께 바꿔야 효과가 확실함 (즉 사실상 옵션1/2와
  조합해야 함). 이상치를 얼마나 눌러줄지 체감 효과는 실제로 붙여봐야 앎.

### 옵션 4. 장르 유사도를 추천 로직에 반영 (하이브리드)

- 장점: 오디오 특성만으론 못 잡는 "장르적 느낌"까지 반영해서 체감 품질이
  가장 좋아질 가능성이 큼. 위 1~3번 중 아무거나와 조합 가능 (예: 오디오
  거리 70% + 장르 일치 가중치 30%처럼).
- 단점: 구현 난이도가 제일 높음 (장르 간 "가까움"을 어떻게 정의할지부터
  설계 필요 — 예: 같은 장르 vs 상위 카테고리가 같은 장르 vs 완전 다른 장르).
  당장 급한 버그 수정보다는 스코프가 커서, 지금 바로 하기보다 stage3
  하이브리드 추천 작업으로 미루는 것도 방법.

## Stage 3 제안

지금 stage2는 "곡 기반 유사곡 추천"과 "상황 텍스트 기반 추천" 두 기능이
CLI로 동작하는 단계까지 왔음. `context_parser.py` 주석에도 이미 "나중에
텍스트 분류 모델로 교체 가능하게 인터페이스를 설계했다"고 적어뒀던 만큼,
자연스러운 stage3 후보는 아래 세 갈래로 보임.

1. **추천 엔진 고도화 (하이브리드)** — 위 옵션4(장르 반영)를 본격적으로
   여기서 진행. 오디오 특성 + 장르 + (가능하면) 인기도/최신성까지 반영하는
   스코어링으로 확장.
2. **상황 텍스트 파서 고도화** — 지금의 키워드 사전 방식(rule-based) 대신
   문장 임베딩(예: sentence-transformers) 기반으로 "비슷한 의미의 다른
   표현"도 인식하게 만들기. 예: "기분이 다운돼"를 사전에 안 넣어도 "우울"과
   비슷하다고 인식.
3. **UI/제품화** — CLI를 벗어나서 웹 UI(Streamlit 등)로 전환하고,
   `spotify_client.py`에 이미 만들어둔 검색 기능으로 앨범아트·재생링크 같은
   메타데이터를 붙여서 실제로 써볼 수 있는 형태로 만들기.

이 셋은 서로 배타적이지 않고 순서대로/병행으로 진행 가능. 만약 stage3에서
1번(하이브리드 추천)을 할 계획이라면 지금 단계에서는 굳이 옵션4까지 다
구현하지 말고 **옵션1(4개 특성+유클리드)로 베이스라인만 빠르게 고쳐두고**
장르 반영은 stage3에서 제대로 설계하는 게 효율적. 반대로 stage3에서
UI/제품화(3번)에 집중할 계획이면, 추천 로직은 지금 최대한 탄탄하게
만들어두는 게 나으므로 **옵션3(RobustScaler, 9개 특성 유지)** 나 **옵션1**
중 하나로 확실히 고정하고 넘어가는 편이 나음.

## 참고: 관련 없는 별개 이슈 (이전에 이미 파악됨)

- `find_song()`에서 아티스트로 필터링했는데 매칭이 하나도 없으면, 아티스트를
  무시하고 title만으로 fuzzy match된 첫 곡을 반환함 (조용히 엉뚱한 곡을
  선택할 위험 있음). 지금 케이스에선 발생 안 했지만 잠재적 버그로 남겨둠.
  → **실제로 재현됨.** 아래 "적용한 수정" 참고.

## 적용한 수정 (2026-08-22)

데이터셋을 새로 찾는 대신, 아래 세 가지를 기존 Kaggle 데이터셋 위에서 고쳤음.

1. **`build_dataset.py`**: 장르당 150곡 캡을 없앰. 인기도 40 이상 필터만 거친
   전체 33,992곡을 그대로 사용하도록 변경 (`stratified_sample(n_per_genre=None)`).
   결과적으로 `stage2_dataset.csv`가 16,804곡 → 33,992곡으로 늘어남.
2. **`recommend.py`**: 코사인 유사도 → 유클리드 거리로 교체. 그리고 "같은 장르
   우선 필터링"을 추가함 — 같은 장르 후보가 `MIN_GENRE_POOL`(15)곡 이상이면
   그 안에서만 랭킹을 매기고, 부족하면 자동으로 전체 데이터셋으로 확장하면서
   그 사실을 사용자에게 경고 문구로 알려줌. 결과 컬럼도 `similarity`(클수록
   유사) → `distance`(작을수록 유사)로 바뀜.
3. **`find_song()` 버그 수정**: 테스트 중 실제로 재현됨 — `lovely`를
   Billie Eilish로 검색했더니 진짜 곡("lovely (with Khalid)", pop)이 아니라
   전혀 다른 동명곡("Lovely" by Lauren Babic;Seraphim, hardcore)이 조용히
   선택돼서 하드코어 음악이 추천되는 문제가 실제로 발생했음. 이제 아티스트가
   주어지면 그 아티스트의 곡들로 먼저 좁힌 뒤 제목을 fuzzy match하도록 순서를
   바꿔서 해결함.

### 수정 후 재검증 결과

- `bad guy` / Billie Eilish → 1위가 `bad guy (with Justin Bieber)`(같은 곡의
  피처링 버전), 8위에 Billie Eilish의 다른 곡(`I Didn't Change My Number`)도 등장.
- `lovely` / Billie Eilish → 버그 수정 후 정상적으로 `lovely (with Khalid)`(pop)를
  찾았고, Olivia Rodrigo(`traitor`, `drivers license`), AURORA(`Runaway`) 등
  실제로 비슷한 무드의 아티스트가 상위에 나옴.
- `blinding lights` / The Weeknd → Taylor Swift(`Wildest Dreams`,
  `Don't Blame Me`), The Weeknd 본인 곡(`Die For You`) 등 pop 장르 내에서
  합리적인 추천이 나옴.
- 장르 후보가 15곡 미만인 경우(`Por Una Cabeza` — tango, 후보 2곡) fallback도
  정상 작동 확인함 (경고 문구 출력 후 전체 데이터셋에서 탐색).

## 추가 이슈: 상황 텍스트 모드("비 오고 차분함") 결과가 장르 뒤섞임 (2026-08-22)

### 증상

곡 제목 기반 모드는 위 수정 후 잘 나오는데, 상황 텍스트 모드("비 오고 차분함")는
여전히 cantopop, turkish, trip-hop, show-tunes(x2), acoustic, pop, club, gospel
등 완전히 다른 장르/언어권이 뒤섞여 나왔음.

### 원인

곡 제목 기반은 "검색한 곡의 장르"라는 기준(앵커)이 있어서 그걸로 후보를 먼저
좁힐 수 있는데, 상황 텍스트 기반은 기준 곡이 없어서 앵커가 없음. 그래서 33,992곡
전체(111개 장르)를 대상으로 순수 오디오 수치 거리만으로 찾다 보니 장르가 흩어짐.

"곡 제목 모드처럼 상위 후보 중 다수결로 장르 하나를 자동으로 고르자"는 방식도
시도해봤는데(top-N 컨센서스), 실측해보니 K값(50/100/200/300)에 따라
mandopop/cantopop/acoustic이 거의 동률로 왔다갔다 해서 불안정했음 — 폐기.

### 적용한 수정

`recommend.py`에 `MOOD_GENRES` 화이트리스트를 추가함. acoustic, chill, pop,
indie, folk, singer-songwriter, r-n-b, soul, rock 계열 등 "무드 음악"으로 흔히
듣는 장르만 후보로 남기고, show-tunes/gospel/kids/disney/comedy/sleep/opera 같은
니치 카테고리와 cantopop/mandopop/turkish/k-pop/j-pop 같은 언어권 장르는 기본
제외함 (화이트리스트 후보가 너무 적어지면 전체 데이터셋으로 자동 확장하는
안전장치도 같이 넣음). 원하면 `MOOD_GENRES` 셋에 장르를 추가/제거해서 조정 가능.

재검증: "비 오고 차분함" → Ciaran Lavery(`Shame`), The Rescues(`Teenage Dream`),
FKA twigs(`meta angel`), Penny and Sparrow(`Eloise`) 등 acoustic/chill/indie
계열로 통일감 있게 나옴. "신나고 조깅하는 중"은 hard-rock/funk/ska/punk 등
여러 장르가 섞여 나오는데, 이건 "에너지 넘치는 무드"라는 게 애초에 여러 장르에
걸쳐있는 개념이라 자연스러운 결과로 판단함 (니치/언어권 카테고리는 안 나옴).

## 기능 추가: 상황 텍스트 모드에 선택적 가수 입력 (2026-08-22)

상황 텍스트 모드(2번)에서 문장 입력 다음에 "선호하는 가수"를 물어보도록 추가함.
비워두면 기존처럼 `MOOD_GENRES` 화이트리스트로 찾고, 가수를 입력하면 그 가수가
걸쳐있는 장르(들)로 후보를 좁혀서 그 안에서 무드 타깃 벡터로 랭킹을 매김
(곡 기반 추천의 "같은 장르 우선 필터링"과 같은 방식을 재사용). 데이터셋에 없는
가수를 입력하면 경고 문구를 띄우고 무드 화이트리스트로 자동 대체함.

재검증: "비 오고 차분함" + "Billie Eilish" → 그녀의 장르(electro, pop) 안에서
Ashe(`Moral of the Story`), Zara Larsson, Alessia Cara, Ed Sheeran 등으로 나옴.
가수 미입력/오타 입력 시 둘 다 기존 무드 화이트리스트 결과로 정상 폴백됨.

## 기능 추가: 가수 본인 곡을 1위로 고정 (2026-08-22, 사용자 요청)

### 발견

"칙칙해" + "Justin Bieber"로 돌려봤더니 그의 곡이 결과에 하나도 안 나옴.
확인해보니 버그가 아니라, 그의 곡 대부분이 밝고 신나는 스타일이라 데이터셋
안에서 "칙칙한" 무드에 제일 가까운 그의 곡도 거리 1.93으로, 전체 34,000곡
중 3114등 수준이었음 (그의 장르 후보 안에서 자연 랭킹으로는 top10에 못 듦).

### 요청 반영

사용자가 "1위는 그 아티스트 곡으로 무조건 고정, 나머지는 기존처럼 비슷한 곡"을
요청해서 그렇게 구현함. `recommend_by_target()`에서 artist가 주어지면 그
아티스트 본인 곡들 중 무드 타깃과 가장 가까운 곡을 찾아서 무조건 1위에 넣고
(원래 랭킹에서 몇 등이었든 상관없이), 나머지 자리는 기존 로직(아티스트 장르
앵커 → 화이트리스트 → 전체 데이터셋 순으로 폴백)으로 그대로 채움. 1위 곡이
"나머지" 랭킹에 중복으로 다시 뽑히지 않도록 제외 처리함.

재검증: "칙칙해" + "Justin Bieber" → 1위 `Let Me Love You - Sean Paul Remix`
(그의 곡, 거리 1.928, 원래 랭킹상 거리가 제일 멀어서 순위 밖이었던 곡이지만
그의 곡 중엔 최선), 2위부터는 기존처럼 Ashe/Sam Smith 등 장르 기준 추천이
이어짐. 가수 없이/못 찾는 가수로 돌리면 이전처럼 정상 동작(회귀 확인함).

### 남은 참고사항

- `recommend_by_target()`(상황 텍스트 기반 추천)은 "기준이 되는 장르"가 없어서
  장르 필터링은 적용 안 함. 유클리드 거리로만 교체함.
- 여전히 오디오 특성 9개 + 장르 라벨만으로 판단하기 때문에, Kaggle 장르 태깅
  자체가 애매하거나(한 곡이 실제로는 여러 장르에 걸쳐있는데 태그는 하나뿐)
  드물게 카테고리가 이상한 경우(`lovely`가 hardcore로 잘못 태깅됐던 것처럼)는
  여전히 남아있을 수 있음 — 이건 데이터셋 자체의 한계라 stage3에서 메타데이터
  보강(Spotify 검색 등)으로 보완하거나 그냥 감안하고 가는 수밖에 없음.

## stage4-2b: 임베딩 기반 상황 텍스트 파서 — 시작하다가 발견한 네트워크 제약 (2026-08-31)

### 하려던 것

`context_parser.py`의 `KEYWORD_RULES` 부분일치(`if keyword in text`) 매칭을
`sentence-transformers` 사전학습 다국어 임베딩(`paraphrase-multilingual-MiniLM-L12-v2`
후보)으로 바꿔서, 키워드 사전에 없는 표현("장맛비라 꿀꿀하다" 처럼 "비"/"칙칙"
같은 정확한 단어가 없는 paraphrase)도 의미적으로 가장 가까운 키워드를 찾아
매칭되게 하려고 함.

### 막힌 지점

1. **원격 클라우드 실행 환경**: `sentence-transformers`는 pip로 설치는 되는데
   (PyPI는 네트워크 허용 목록에 있음), 실제 모델 가중치를 받으려고
   `SentenceTransformer(...)`를 호출하면 `huggingface.co`가 아웃바운드
   허용 목록에 없어서 403으로 막힘 (`egress proxy` 확인함).
2. **"내 컴퓨터에서 실행" 기능도 실제로는 진짜 macOS 터미널이 아니라, 파일
   폴더만 마운트되는 별도의 샌드박스 Linux VM이라는 걸 이번에 확인함.** 이
   VM도 클라우드 실행 환경과 똑같은 네트워크 허용 목록을 쓰고 있어서
   `huggingface.co`, `download.pytorch.org`, `api.openai.com`이 전부
   막혀있음 (`pypi.org`, `github.com`은 열려있음).
   → 즉 이 원격 개발 환경들로 접근할 수 있는 어떤 곳에서도 Hugging Face
   Hub에서 모델 가중치를 직접 받을 수 없음. 이건 원격 실행 환경 자체의
   네트워크 제약이지, 내 실제 Mac이 인터넷이 안 되는 게 아님.
3. (부가 발견) 이 VM의 디스크가 9.8GB로 작은데, `pip install
   sentence-transformers`가 기본으로 Linux+CUDA용 torch를 끌어오면서
   nvidia-* 패키지들(cublas/cudnn/nccl 등 총 2GB+)까지 같이 받아서 디스크가
   꽉 참 (`No space left on device`). CPU 전용 torch(`--index-url
   https://download.pytorch.org/whl/cpu`)로 우회하려 했지만 그 도메인 자체가
   막혀있어서 이것도 안 됨. (실제 내 Mac에서 직접 설치하면 이 문제는
   안 생김 — macOS는 애초에 CUDA용 torch가 없어서 가벼운 버전만 받음.)

### 결론

원격 개발 환경(클라우드 실행 환경, "내 컴퓨터에서 실행" VM) 중 어느 쪽으로도
실제 임베딩 모델을 다운받아 테스트할 수 없음. 이 부분은 내 Mac의 진짜
터미널(Terminal.app)에서 한 번 설치를 직접 진행해야 함. 어떤 방식으로
진행할지 결정 필요 (옵션: (a) 로컬에서 직접 설치하는 방식으로 진행, (b) 외부
모델 다운로드가 필요 없는 더 가벼운 대안 방식으로 방향 전환, (c) 일단 보류).


### 결정 및 구현 (2026-08-31)

세 옵션(로컬 직접 설치 / 가벼운 대안으로 전환 / 보류) 중 **"로컬 직접 설치"**로
결정함. 코드/통합 로직까지 다 작성해두고, 실제 모델 다운로드 + 품질 검증만
내 Mac 터미널에서 진행하는 방식.

**구현 내용 (`context_parser.py`):**
- 기존 `KEYWORD_RULES` substring 매칭은 1단계로 그대로 유지 (회귀 없음 보장).
- 2단계로 `_semantic_matches()` 추가: 1단계에서 못 잡은 키워드에 한해 문장
  임베딩 vs 키워드 임베딩 코사인 유사도 비교. `EMBEDDING_SIMILARITY_THRESHOLD`
  (잠정값 0.5) 이상이면 매칭으로 인정.
- 동의어 delta 중복 방지: delta 값이 완전히 같은 키워드끼리 "개념 그룹"으로
  묶어서(`_build_concept_groups`), 그룹당 최댓값 유사도 1번만 적용되게 함
  (예: "신남"/"신나"가 동시에 threshold를 넘겨도 delta가 2배로 더해지지 않음).
- `sentence-transformers` 미설치 시 `ImportError`를 잡아서 조용히 1단계
  결과만 반환 - 즉 설치 전/후 모두 정상 동작, 기존 테스트했던 결과 그대로
  재현됨(로컬에 sentence-transformers 없는 이 환경에서 `context_parser.py`
  단독 실행으로 회귀 확인함: "오늘 비오고 기분 칙칙해" -> 매칭 키워드
  `['칙칙', '비']`, 목표값 기존과 동일).
- `EMBEDDING_MODEL_NAME = "paraphrase-multilingual-MiniLM-L12-v2"` (한국어
  포함 다국어 지원, 비교적 가벼운 모델).

**새 파일:**
- `calibrate_embedding_threshold.py` — threshold 검증용. 사전에 없는
  paraphrase 문장 8개를 실제 모델로 임베딩해서 각 키워드 그룹과의 유사도를
  출력함. 로컬에서 이걸 돌려보고 나온 숫자로
  `EMBEDDING_SIMILARITY_THRESHOLD` 값을 조정하면 됨 (원격 개발 환경에서는
  모델을 실제로 돌려볼 수 없어서 이 값을 실증적으로 정하지 못했음 - 반드시
  로컬 검증 필요).
- `requirements-embedding.txt` — `sentence-transformers`만 따로 분리(무거운
  선택적 의존성이라 기본 `requirements.txt`에는 안 넣음).

**로컬에서 할 일 (진짜 Mac Terminal.app에서):**
```
cd stage2
pip3 install -r requirements-embedding.txt   # 처음 한 번, torch 포함이라 용량 큼
python3 calibrate_embedding_threshold.py     # threshold 검증, 필요하면 조정
streamlit run app.py                          # 평소처럼 실행, 이제 임베딩 매칭도 같이 동작
```

**참고:** `requirements.txt`(기존 핵심 의존성)만 설치한 사람은 이 기능 없이도
앱이 정상 동작함 - 리크루터가 GitHub에서 받아서 가볍게 돌려볼 때도 문제 없게
하려는 의도.

### 다음 후보
- min이 로컬 설치 + `calibrate_embedding_threshold.py` 실행 후 결과를 공유하면,
  그 숫자 보고 threshold 최종 조정 + Streamlit 앱에서 실제 paraphrase 문장으로
  재검증(예: "장맛비라 꿀꿀하다"가 실제로 비/우울 계열 곡을 추천하는지).
- 검증 끝나면 stage4 나머지 항목(재랭킹 모델, K-means 부활, 정량 평가)로 이동.

## stage4-2c: 임베딩 매칭 1차 검증에서 발견한 버그 — 짧은 키워드 원문 비교의 함정 (2026-08-31)

### 증상

min이 로컬에서 `calibrate_embedding_threshold.py`(1차 버전)를 처음 실행한
결과, 완전히 무관한 문장("오늘 저녁 뭐 먹을지 고민중이야")도 "신나"라는
키워드와 유사도 0.933이 나옴 - 실제 "신남/행복" 계열 문장("완전 럭키비키한
기분이야")의 유사도 0.914보다도 오히려 더 높았음. 다른 무관한 문장들도
전부 "신나"와 0.9 이상으로 나와서, 어떤 threshold를 잡아도 관련 없는
문장과 진짜 매칭 문장을 구분할 수 없는 상태였음.

### 원인

1차 버전은 문장 임베딩을 키워드 원문("신나", "비", "느리게" 같은 2~3글자
단어) 임베딩과 직접 비교했음. `paraphrase-multilingual-MiniLM-L12-v2`같은
SBERT 계열 모델은 "문장 대 문장"의 의미적 유사도(STS) 학습에 최적화돼
있어서, 문맥이 없는 짧은 단어 하나를 비교 대상으로 넣으면 그 임베딩이
특정 방향으로 쏠려서(비등방성/hubness 문제로 추정) 입력이 뭐든 상관없이
유사도가 높게 나오는 경향이 있었던 것으로 판단함. 즉 모델이나 코드가
고장난 게 아니라, "짧은 키워드 vs 문장" 비교 자체가 이 모델의 정상
사용법이 아니었던 것.

### 적용한 수정

`context_parser.py`에 `CONCEPT_EXAMPLE_SENTENCES` 사전을 추가해서, 각
개념(동의어 그룹)마다 자연스러운 예문 2~4개("완전 신난다", "기분이 우울해"
등)를 직접 작성함. 임베딩 비교를 "입력 문장 vs 키워드 원문" 대신 "입력
문장 vs 그 개념의 예문들" 방식으로 바꿈 (그룹 내 예문 중 최댓값 유사도
사용). `calibrate_embedding_threshold.py`도 같은 방식으로 업데이트하고,
무관한 테스트 문장을 3개로 늘리고 "관련 없음 최댓값 vs 실제 매칭 최솟값"
자동 비교/추천 threshold 출력 기능을 추가함.

### 재검증 필요 (다음 단계)

이 수정은 아직 실제 모델로 안 돌려봄 - 원격 개발 환경에서는 huggingface.co가
막혀서 여전히 테스트 불가(stage4-2b 참고). 로컬에서
`calibrate_embedding_threshold.py`를 다시 돌려서 이번엔 "관련 없음" 문장의
유사도가 실제 매칭 문장보다 확실히 낮게 나오는지 확인 필요. 만약 이번에도
분리가 안 되면, 예문을 더 다양하게 늘리거나 모델을 다른 걸로 바꾸는 걸
고려해야 함.

## stage4-2d: 2차 검증에서도 무관한 문장이 안 걸러짐 — 모델을 한국어 전용으로 교체 (2026-08-31)

### 증상

예문 방식(stage4-2c)으로 고친 뒤 min이 다시 로컬에서 돌려봄. 일부 항목은
유사도 1.000이 나왔는데 확인해보니 내가 테스트 문장(`TEST_CASES`)과 예문
(`CONCEPT_EXAMPLE_SENTENCES`)을 거의 똑같이 써놔서 생긴 내 실수(예: "차분"
예문 "조용히 마음을 가라앉히고 싶어"가 테스트 문장이랑 완전히 같은 문장이었음,
"비" 예문 "장맛비라 꿀꿀해"도 테스트 문장 "장맛비라 꿀꿀하다"랑 사실상 동일)
- 이 항목들의 높은 점수는 진짜 의미 매칭이 아니라 그냥 문자열이 겹친 것.

그런데 그 오염된 항목들을 빼고 "관련 없음" 카테고리만 봐도 문제가 남아있었음:
"오늘 저녁 뭐 먹을지 고민중이야"(0.977), "이 코드 왜 안 돌아가지"(0.965) 등
완전히 무관한 문장도 대부분의 개념과 0.93~0.97로 나옴 - 실제 매칭돼야
하는 문장 최저치(0.809)보다도 높아서 threshold로 분리가 안 됨.

### 원인

`paraphrase-multilingual-MiniLM-L12-v2`는 50개 언어를 하나의 작은 모델로
커버하는 범용 다국어 모델임. 언어별 학습 데이터 비중이 균등하지 않아서
한국어처럼 상대적으로 데이터가 적은 언어는 임베딩 품질이 떨어질 수 있고,
이번 케이스에서는 한국어 문장끼리 의미와 상관없이 코사인 유사도가 전반적으로
높게 뭉치는(붕괴하는) 현상으로 보임. 즉 코드 로직 문제가 아니라 "한국어
전용 작업에 다국어 범용 모델을 썼다"는 모델 선택 자체의 문제로 판단함.

### 적용한 수정

`EMBEDDING_MODEL_NAME`을 `jhgan/ko-sroberta-multitask`로 교체함. 이 모델은
한국어 STS(KorSTS)/NLI(KorNLI) 데이터로 직접 멀티태스크 학습되고 KorSTS
기준 Cosine Spearman 85.6을 기록한, 한국어 문장 유사도 작업에 흔히 쓰이는
전용 모델 (참고: https://huggingface.co/jhgan/ko-sroberta-multitask).
동시에 `CONCEPT_EXAMPLE_SENTENCES`를 테스트 문장(`TEST_CASES`)과 안 겹치게
싹 정리해서, 다음 검증부터는 공정하게(문자열 우연 일치 없이) 결과를 볼 수
있게 함.

### 재검증 필요 (다음 단계)

아직 실제로 안 돌려봄 - 여전히 원격 개발 환경에서는 huggingface.co가
막혀있어서(stage4-2b) 테스트 불가. 로컬에서 `calibrate_embedding_threshold.py`
다시 실행 필요 (새 모델이라 처음엔 다시 다운로드됨). 이번에도 "관련 없음"
문장들의 유사도가 실제 매칭 문장보다 확실히 낮게 안 나오면, 한국어 임베딩
모델 자체보다는 "단문 vs 단문" 비교 방식(코사인 유사도의 근본적인 anisotropy
문제)을 의심하고 정규화(예: 문장 쌍 평균을 빼는 whitening/centering)나
아예 다른 접근(예: KoNLPy 형태소 분석 기반 키워드 확장)으로 방향 전환 검토.

## stage4-2e: 모델 교체 후 재검증 성공 — threshold 확정 (2026-08-31)

### 결과

min이 로컬에서 `jhgan/ko-sroberta-multitask`로 `calibrate_embedding_threshold.py`를
다시 돌림. 이번엔 명확하게 분리됨:

- 실제 매칭돼야 하는 문장(7개)들의 top1 유사도: 0.548 ~ 0.805
- 관련 없는 문장(3개)들의 top1 유사도: 0.209 ~ 0.345
- 둘 사이에 겹치는 구간이 없음 (최저 매칭 0.548 > 최고 무관 0.345)

개별 결과도 기대한 개념과 대체로 일치함: "하늘이 완전 쨍하고 좋다" -> 맑음
(0.600, 1순위), "천천히 동네 한 바퀴 걷는 중" -> 산책(0.800), "지금 헬스장에서
러닝머신 뛰는 중" -> 뛰면서(0.627), "조용히 마음을 가라앉히고 싶어" -> 차분
(0.805) 등 전부 정답. "그냥 다 귀찮고 축 처지는 하루야"만 "다운"(0.575)이
"우울"(0.551)보다 근소하게 높게 나왔는데, 둘 다 델타값이 거의 같은
부정적 감정 계열이라 실질적으로 문제 없음.

### 결정

`EMBEDDING_SIMILARITY_THRESHOLD`를 두 구간의 중간값인 **0.45**로 확정함
(무관한 문장 최고치 0.345, 실제 매칭 최저치 0.548 사이). 이전에 근거 없이
잠정 설정했던 0.5도 사실 이 구간(0.345~0.548) 안에 들어가서 우연히
안전하긴 했지만, 이번엔 실측 데이터로 뽑은 값으로 교체함.

### 결론: stage4-2(임베딩 기반 상황 텍스트 파서) 핵심 검증 완료

- 1단계(substring) + 2단계(임베딩 의미 매칭) 구조 정상 동작 확인.
- 시행착오 요약(다른 프로젝트 설명할 때 포인트): (1) 짧은 키워드 원문 대신
  문장형 예문으로 비교해야 SBERT가 제대로 동작함 (2) 다국어 범용 모델보다
  타깃 언어 전용 모델이 훨씬 안정적임 (3) threshold는 감이 아니라 관련/무관
  샘플의 유사도 분포를 실측해서 정해야 함. 세 가지 다 실제로 겪고 고친
  과정이라 recruiter 인터뷰에서 "왜 이렇게 설계했는지" 근거 있게 설명 가능.

### 다음 후보

- Streamlit 앱(`app.py`)에서 실제로 사전에 없는 표현("장맛비라 꿀꿀하다" 등)을
  입력해서 최종 추천 결과까지 End-to-End로 재검증 (지금까지는
  `calibrate_embedding_threshold.py`로 유사도 숫자만 확인했고, 실제 추천곡이
  잘 나오는지는 아직 확인 안 함).
- 검증 끝나면 stage4 나머지 항목(재랭킹 모델, K-means 부활, 정량 평가) 또는
  GitHub 업로드로 이동.

## 버그: requirements.txt의 streamlit 버전 지정 오류 (2026-08-31)

### 증상

min이 로컬에서 `pip3 install -r requirements.txt` 실행 시
`ERROR: Could not find a version that satisfies the requirement streamlit>=1.62.0`
발생. PyPI에 실제로 존재하는 최신 버전은 1.50.x까지라 1.62.0은애초에
존재하지 않는 버전이었음 - stage3 때 requirements.txt에 잘못된 버전을
적어놨던 실수.

### 원인 및 수정

`streamlit>=1.62.0` -> `streamlit>=1.32.0`로 수정. 이 프로젝트가 실제로
쓰는 기능들(`st.cache_resource`/`st.cache_data`는 1.18+, `st.tabs`는
1.11+, `st.container(border=True)`는 1.25+, `st.rerun()`은 초기 버전부터
지원) 기준으로 충분히 낮은 버전이면서 안전한 하한선으로 잡음.

### 교훈

이건 원격 개발 환경에서는 못 잡아낸 버그임 - 클라우드 실행 환경/VM 양쪽 다
PyPI에 실제로 존재하는 버전 목록을 그때그때 검증 안 하고 버전 문자열만
적어놓다 보니 생긴 실수. requirements.txt 같은 배포용 설정 파일은 직접
로컬에서 `pip install -r requirements.txt`로 한 번 실행해봐야 이런 문제를
잡을 수 있다는 걸 다시 확인함 - GitHub 올리기 전에 이 파일로 한 번 더 클린
환경(가상환경 등)에서 설치 테스트해보는 게 좋겠음.

## stage4-2f: Streamlit 앱에서 End-to-End 재검증 완료 (2026-08-31)

### 검증 방법

min이 로컬에서 `python3 -m streamlit run app.py`로 실제 앱을 띄우고, "지금
기분으로 찾기" 탭에 사전에 없는 표현 **"장맛비라 꿀꿀하다"**를 입력해서
실제 추천 결과 확인.

### 결과

"인식된 키워드: 비, 장마(의미유사 0.47, 예문:'장마철이라 계속 비만 와서
우중충해')"로 표시됨:

- **비**: "장맛비라"에 "비" 글자가 문자 그대로 포함돼있어서 1단계(substring)로
  잡힘 (기존 동작 그대로).
- **장마**: "장맛비라 꿀꿀하다"에 "장마"라는 문자열은 없는데도(장맛비 ≠
  장마), 2단계 임베딩 매칭이 유사도 0.47로 잡아냄 - 방금 확정한 threshold
  0.45를 실제로 넘긴 실사용 사례. **stage4-2의 핵심 목표(사전에 없는
  paraphrase도 의미적으로 인식)가 실제 앱에서 최초로 확인됨.**

추천된 곡(Raye, Jhuk Na Paunga, Aaina 등)은 `MOOD_GENRES` 화이트리스트
기반 무드 추천 로직(기존에 이미 검증됐던 부분, 가수 미지정 시 전체
화이트리스트 장르에서 거리순 추천)을 그대로 탄 것 - 이번 임베딩 매칭 추가와는
무관한 기존 동작이라 별도 이슈 아님.

### 결론

stage4-2(임베딩 기반 상황 텍스트 파서) End-to-End 검증까지 완료. 다음은
stage4 나머지 항목(피드백 기반 재랭킹 모델, K-means 취향 클러스터링 부활,
정량 평가) 또는 GitHub 업로드 중 다음 우선순위 결정 필요.

## GitHub 업로드 완료 (2026-08-31)

### 작업 내용

로컬 프로젝트 폴더를 git 저장소로 초기화하고 GitHub에 처음 올림.

1. `git init` (원격 개발 환경에서 실행 - 실제 폴더가 로컬에 마운트돼있어서
   실제 파일에 바로 반영됨). git 커밋 작성자 정보(이름/이메일)를 설정함 -
   이메일은 berkeley.edu 계정 사용을 선택함 (GitHub 계정에 등록/인증된
   이메일과 일치해야 커밋이 프로필에 제대로 연결된다는 점 확인하고 결정).
2. `README.md` 새로 작성 - 프로젝트 개요, 주요 기능, 기술 스택, 설치/실행
   방법(Kaggle 원본 데이터셋은 저장소에 포함 안 하므로 직접 받아서
   `build_dataset.py`로 생성해야 함을 명시), 개발 히스토리 요약, `ISSUES.md`
   링크 포함.
3. `.gitignore` 재확인: `data/*.csv`(원본+정제 데이터셋, 각각 20MB/5.7MB),
   `.env`(실제로는 존재하지 않았음, `.env.example`만 커밋됨) 정상 제외 확인.
   `feedback.csv`는 의도적으로 포함(프로젝트 자체가 만든 학습용 데이터).
4. 첫 커밋(`f4e5ac5`, 14개 파일, 1851줄) 생성.
5. GitHub에서 빈 저장소(`mood-song-recommender`, Public, README/gitignore/
   license 전부 미체크) 생성 후, min이 본인 Mac 터미널에서
   `git remote add origin` + `git push -u origin main`으로 push.
6. push 후 GitHub 저장소 파일 목록과 커밋 해시(`f4e5ac5`)가 로컬과 정확히
   일치함을 확인함.

### 결론

프로젝트가 이제 https://github.com/Hyoungmin2/mood-song-recommender 에
공개돼있음. 다음 단계 후보: Streamlit Community Cloud 배포(설치 없이
누구나 접속해서 임베딩 기능까지 체험 가능하게), 또는 stage4 나머지 ML
항목(재랭킹 모델, K-means 부활, 정량 평가) 진행.

## 아이디어(보류): 데이터셋에 없는 곡 요청 기능 (2026-08-31)

min이 제안: 곡 검색에서 못 찾은 곡을 사용자가 직접 입력/저장할 수 있게
하면 어떨지 논의함. 핵심 난제는 라벨링 - Spotify `audio-features` API가
막혀있어서(위 "설계 결정 메모" 참고) 새로 입력받은 곡의 진짜 오디오
특징값을 자동으로 얻을 방법이 없음. 검토한 옵션 세 가지:

1. 요청 로그만 기록 (추천 풀에는 안 넣음, 나중에 데이터셋 보강 우선순위
   파악용)
2. Spotify search로 아티스트/장르만 가져와서, 데이터셋에 있는 같은
   아티스트 곡들의 평균 특징값을 "추정치"로 추천 풀에 포함
3. 일단 입력 UI + 저장 기능만 만들고 라벨링 전략은 나중에 결정

**결정: 지금은 보류.** 나중에 다시 논의할 것.

## 버그: 같은 곡에 여러 번 좋아요 누르면 feedback.csv에 중복 기록 (2026-08-31)

### 증상

min이 앱을 쓰다가 같은 곡에 좋아요를 여러 번 눌렀는데 "내 피드백 개수"가
누른 횟수만큼 계속 올라가는 걸 발견함. 실제로 `feedback.csv`를 열어보니
"10,000 Hours (with Justin Bieber)"가 같은 문장(상황) 기준으로 04:16:44 /
04:16:48 / 04:16:50, 몇 초 간격으로 3번 연속 "like"로 중복 기록돼있었음.

### 원인

`render_result_cards()`에서 이미 투표한 카드도 👍/👎 버튼이 그대로
남아있었음 - "🙏 좋아요 기록됨" 캡션만 옆에 추가로 뜰 뿐, 버튼 자체는
안 사라져서 다시 누르면 `append_feedback()`이 매번 새 줄을 추가함
(단순 append 방식이라 중복 방지 로직이 아예 없었음).

### 적용한 수정

1. `render_result_cards()`: 이미 투표한 카드는 좋아요/스킵 버튼을 아예
   숨기고 "기록됨" 문구 + "변경" 버튼(투표를 취소하고 다시 고를 수 있게)만
   보여주도록 변경.
2. `append_feedback()`: 저장 단에서도 안전장치 추가 - 매번 파일 전체를
   읽어서 같은 `(user_id, source, context, track_name, artists)` 조합의
   기존 행을 지우고 새 행으로 덮어씀(최신 투표 유지). 세션이 초기화되는
   경우(새로고침, 재실행)에도 중복이 안 쌓이게 하기 위한 이중 방어.
   `/tmp`에서 로직만 분리해 유닛 테스트로 검증함: 같은 곡+같은 상황에
   3번 append해도 실제로는 1줄만 남고, 상황(context)이 다르면 별도로
   기록되는 것 확인.
3. 기존 `feedback.csv`에 이미 쌓여있던 중복 2줄(위 "10,000 Hours" 사례)을
   정리 - 가장 마지막 기록만 남기고 총 13줄 -> 11줄로 정리함.

### 재검증 필요

로직 자체는 유닛 테스트로 확인했지만, 실제 Streamlit 앱에서 같은 카드
여러 번 눌러도 버튼이 잘 사라지고 "변경" 버튼도 정상 동작하는지는 아직
실제 앱에서 확인 안 함 - 다음에 앱 켤 때 확인 필요.

## 버그: Streamlit Cloud 배포 시 FileNotFoundError (2026-08-31)

### 증상

로컬에서 정상 동작하던 앱을 Streamlit Community Cloud에 배포한 후,
"곡으로 찾기" 탭은 정상 동작했지만 "지금 기분으로 찾기" 탭에서 추천받기를
누르면 `FileNotFoundError`가 발생함. 트레이스백 확인 결과
`recommend.py`의 `pd.read_csv(dataset_path)`에서 발생.

### 원인

`.gitignore`에 있던 `data/*.csv` 규칙이 원본 Kaggle 데이터셋뿐 아니라
앱이 실제로 로드하는 가공 데이터셋(`data/stage2_dataset.csv`, 33,992곡)
까지 같이 제외시켜버렸음. 로컬 개발 환경에는 파일이 실제로 디스크에
있었기 때문에 문제없이 동작했지만, Streamlit Cloud는 GitHub 저장소를
그대로 clone해서 실행하는 방식이라 애초에 추적되지 않은 파일은 존재하지
않음. `git ls-files data/`로 확인해보니 실제로 `data/` 아래 아무 파일도
git에 추적되고 있지 않았음.

### 적용한 수정

`.gitignore`에 `!data/stage2_dataset.csv` 예외 규칙을 추가해서 가공
데이터셋만 git 추적 대상에 포함시킴(5.7MB, GitHub 100MB 제한과 무관,
BSD 라이선스 원본 기반이라 재배포에 문제없음). 원본 다운로드 파일
(`spotify_tracks_dataset.csv`, 20MB, `build_dataset.py`를 돌릴 때만
필요하고 앱 실행에는 불필요)은 계속 `.gitignore`로 제외 유지. 커밋
`3b845c6`.

### 재검증 결과 (2026-08-31)

push 후 Streamlit Cloud 재배포 확인. "지금 기분으로 찾기" 탭에서
"장맛비라 꿀꿀하다" 입력 시 에러 없이 정상 동작하고, "인식된 키워드:
비, 장마(의미유사 0.47, 예문:'장마철이라 계속 비만 와서 우중충해')"
문구까지 그대로 떠서 임베딩 기반 의미 매칭도 클라우드 환경에서 정상
작동함을 확인함. stage4-2 End-to-End 검증이 로컬뿐 아니라 실제 배포
환경에서도 완료됨.

## 배포 앱 피드백 다운로드 버튼 추가 (2026-08-31)

### 배경

Streamlit Cloud의 로컬 파일시스템이 임시(ephemeral)라는 걸 확인함(공식
문서/커뮤니티 포럼 기준). 앱이 재부팅되면(12시간 무접속 sleep, 새 커밋
push로 인한 재배포, 리소스 초과 강제 재시작) `feedback.csv`에 쌓인
내용이 git에 커밋된 마지막 스냅샷으로 초기화됨. 무료 티어는 서버
컨테이너에 SSH나 파일 탐색기로 직접 접근할 방법이 없어서, 배포된 앱을
써본 다른 사람들의 피드백을 재부팅 전에 빼올 방법이 없었음.

### 적용한 수정

사이드바에 `st.download_button`으로 현재 서버의 `feedback.csv`를
다운로드하는 버튼 추가. 흐름: 다른 사람이 앱 써봄 -> 재부팅되기 전에
사이트 들어가서 다운로드 버튼으로 최신 `feedback.csv` 받음 -> 로컬
파일과 합쳐서 git commit/push -> 영구 보관.

### 참고

이 방식은 "누군가 잊지 않고 주기적으로 다운로드해야 한다"는 수동
운영에 의존함. 피드백 양이 늘어나서 더 안정적인 저장이 필요해지면
외부 저장소(Google Sheets, 간단한 DB 등) 연동으로 교체하는 걸 고려할 것
(위 "Streamlit Cloud 배포" 섹션에서 다룬 임시 파일시스템 이슈와 연결됨).

## 배포 이후 새 피드백 알림 UI 추가 (2026-08-31)

### 배경

다운로드 버튼만 있으면 "지금 새로 쌓인 피드백이 있는지" 매번 직접
확인해야 하는 불편함이 있었음. 재부팅 시점 기준으로 얼마나 늘었는지
한눈에 보이는 표시가 필요했음.

### 구현

`get_baseline_feedback_count()`를 `st.cache_resource`로 감싸서 이번
배포(프로세스 시작) 시점의 피드백 행 수를 한 번만 계산해서 고정함 -
`st.cache_resource`는 앱이 재부팅되기 전까지 값을 유지하는 캐시라
"배포 시점 스냅샷"을 표현하는 데 정확히 들어맞음. 사이드바에서 이
값과 실시간 `count_feedback()`을 비교해서 차이가 있으면
"🔔 배포 이후 새 피드백 N개 - 재부팅 전에 다운로드하세요" 경고를
띄우도록 함. 다운로드해서 merge 후 git push하면 재배포(재부팅)가
일어나서 baseline이 자동으로 최신 값으로 재설정되므로 별도 리셋
로직 없이도 앞뒤가 맞음.

## 버그: 방문자가 Clear cache 누르면 새 피드백 알림이 잘못 리셋됨 (2026-08-31)

### 증상

앱을 쓰다가 Command+C로 텍스트를 복사하려는 과정에서 Streamlit이 "C"
키를 Clear cache 단축키로 인식해서 확인창이 뜨는 걸 발견함. 이 과정에서
"Clear caches"가 개발자 전용 기능이 아니라 **그 앱에 들어온 누구나**
햄버거 메뉴나 C 키로 실행할 수 있는 기능이라는 게 드러남.

### 원인

`get_baseline_feedback_count()`를 `st.cache_resource`로 구현해뒀는데,
이 데코레이터가 만드는 캐시는 Streamlit의 "Clear cache" 메뉴/단축키로
누구나 지울 수 있음. 방문자가 실수로든 의도적으로든 이걸 누르면
기준값이 그 순간의 현재 개수로 재계산돼서, 실제로는 배포 이후 새
피드백이 쌓여있어도 "새로 쌓인 피드백 없음"으로 잘못 표시됨(데이터
자체가 지워지는 건 아니고 알림 표시만 틀려짐).

### 적용한 수정

`st.cache_resource` 대신 Streamlit 캐시 시스템 밖에 있는 일반 모듈
전역 변수(`_baseline_feedback_count`)로 교체. 이건 Clear cache로는
안 지워지고, 앱 프로세스가 실제로 재시작(진짜 재부팅)될 때만
초기화됨 - "배포 시점 스냅샷"이라는 원래 의도에 더 정확히 맞음.

## 피드백 다운로드/병합을 직접 할 수 있도록 스크립트 추가 (2026-08-31)

배포된 앱에서 받은 feedback.csv를 매번 수동으로 병합하지 않아도 되게
`merge_feedback.py`를 추가함. `(user_id, source, context, track_name,
artists)` 기준으로 로컬 파일과 병합하는데, 이건 `app.py`의
`append_feedback()`이 쓰는 것과 동일한 키라 일관됨. 사용법:

```
python3 merge_feedback.py <다운로드한 feedback.csv 경로>
```

실제 다운로드한 서버 데이터(2026-08-31, guest가 "시원하고 기분 좋은
상태"로 테스트한 1개 기록)로 병합 및 스크립트 자체 정상 동작 확인함.

## 한국어/영어 UI 토글 추가 (2026-08-31)

### 배경

리크루터에게 보여줄 때는 영어로 보여줘야 한다는 요구사항. 앱 UI 문구를
아예 영어로 교체하는 대신, 사이드바에서 한국어/영어를 즉시 전환할 수
있는 토글 방식으로 결정함(같은 배포로 한국 사용자도, 영어권 리크루터도
둘 다 대응 가능).

### 설계 결정: 상황 텍스트 매칭 로직은 번역하지 않음

`KEYWORD_RULES`/`CONCEPT_EXAMPLE_SENTENCES`는 한국어 문장에 맞춰 만든
것이고, 이 프로젝트의 핵심(한국어 임베딩 기반 의미 매칭, stage4-2)이라
번역 대상이 아님. 영어 모드에서도 "지금 상황" 입력창은 한국어로 입력해야
의도대로 매칭됨 - 대신 영어 모드에서만 "이 프로젝트의 매칭은 한국어
텍스트용이니 한국어로 입력해보라"는 안내 캡션을 추가로 보여줌.

임베딩 매칭 결과 라벨(예: "장마(의미유사 0.47, 예문:'...')")도 마찬가지로
매칭된 한국어 키워드/예문 자체는 그대로 두고, 감싸는 안내 문구("의미유사"
-> "semantic match", "예문:" -> "example:")만 번역함. 매칭된 원문을
영어로 바꾸면 "무슨 한국어 표현이 감지돼서 매칭됐는지"를 보여줄 수
없어지기 때문.

### 구현

- `app.py`: 문자열 테이블 `T = {key: {"ko": ..., "en": ...}}` +
  `t(key, **kwargs)` 헬퍼 추가. 사이드바에 `st.radio`로 언어 선택,
  `st.session_state["lang"]`에 저장. 모든 정적 UI 문구(제목/캡션/버튼/
  라벨/placeholder)를 `t()`로 교체.
- `recommend.py`: `recommend()`, `recommend_by_target()`에 `lang="ko"`
  파라미터 추가 - 두 함수가 만드는 동적 안내 메시지(note, 예:
  "1위는 '{artist}' 본인 곡 중 가장 가까운 곡으로 고정...")도 언어별로
  분기.
- `context_parser.py`: `parse_context()`, `_semantic_matches()`에
  `lang="ko"` 파라미터 추가. 1단계(substring) 매칭은 언어와 무관하게
  항상 동일(회귀 없음 - 자체 테스트로 확인: `parse_context(..., lang="ko")`
  와 `lang="en"`이 같은 매칭 결과 `['칙칙', '비']`를 반환하는 것 확인).

### 검증

sklearn이 없는 원격 개발 환경이라 `recommend.py`(SongRecommender)는 이
환경에서 직접 테스트 못 함 - `python3 -m py_compile`로 문법만 확인.
`context_parser.parse_context()`는 pandas만으로 되는 1단계(substring)
경로를 이 환경에서 직접 실행해서 `lang="ko"`/`lang="en"` 둘 다 동일한
결과를 내는 것 확인함. 전체 기능(로그인, 검색, 추천, 임베딩 매칭 라벨)은
로컬에서 `streamlit run app.py`로 최종 확인 필요.

## 버그: "시원한 바람이 불어"가 "맑음"으로 잘못 매칭됨 (2026-08-31)

### 증상

min이 배포 앱에서 "시원한 바람이 불어" 입력 -> "인식된 키워드: 맑음
(의미유사 0.53, 예문:'날씨가 맑아')"로 매칭됨. "바람"(바람/시원함)과
"맑음"(맑은 날씨)은 서로 다른 개념인데 잘못 매칭됨.

### 원인

`KEYWORD_RULES`에 "바람"/"시원" 관련 키워드가 아예 등록돼있지 않아서
1단계(규칙 기반)에서 아무것도 못 잡고, 2단계(임베딩 의미 매칭)로
넘어감. 사전학습 모델(`jhgan/ko-sroberta-multitask`) 입장에서는 "바람이
분다"와 "날씨가 맑다"가 둘 다 "날씨를 얘기하는 한국어 문장"이라는 점에서
어느 정도 유사하게 판단한 것으로 보임(유사도 0.53, threshold 0.45를
넘김) - 모델이 큰 범주(날씨)는 잘 구분해도 그 안의 세부 개념(바람 vs
맑음)까지 완벽히 구분하지는 못하는 것으로 판단.

이건 사전학습된 외부 모델의 판단 기준 자체이므로 코드로 직접 수정할 수
있는 부분이 아님 - 다만 이런 케이스를 1단계 규칙으로 등록해서 애매한
임베딩 추측에 의존하지 않게 만드는 건 가능함.

### 적용한 수정

`KEYWORD_RULES`에 "바람"/"시원" 키워드 추가(`{"valence": +0.15,
"acousticness": +0.1, "energy": -0.1}` - 맑음보다는 차분하고 은은하게
상쾌한 느낌으로 임의 설정, 다른 규칙들처럼 실측 검증된 값은 아니고
방향성 기준의 수동 설정임). `CONCEPT_EXAMPLE_SENTENCES`에도 대응하는
예문 추가(임베딩 2단계 폴백용). 이제 "바람"/"시원"이 포함된 문장은
1단계에서 정확히 잡혀서 "맑음"으로 잘못 매칭될 일이 없음.

### 검증

pandas만으로 되는 1단계(substring) 경로를 이 환경에서 직접 실행해서
`parse_context("시원한 바람이 불어", df, use_embedding=False)`가
`['바람', '시원']`을 반환하는 것 확인함(더 이상 임베딩 2단계로 안
넘어감). 기존 회귀 테스트 문장("오늘 비오고 기분 칙칙해" -> ['칙칙', '비'])도
그대로 유지되는 것 확인. `_build_concept_groups()`에서 "바람"/"시원"이
같은 개념(동의어)으로 정상적으로 묶이는 것도 확인함. 실제 임베딩 경로
(sentence-transformers)는 이 환경에 설치가 안 돼있어서 직접 재현/재검증은
로컬에서 필요함.

## 후속: "바람"/"시원" 키워드 추가 후에도 "맑음"이 같이 걸림 (2026-08-31)

### 증상

바로 위 항목에서 "바람"/"시원"을 1단계 키워드로 등록했는데도, 배포된
앱에서 "시원한 바람" 입력 시 "인식된 키워드: 바람, 시원, 맑음(의미유사
0.55, 예문:'날씨가 맑아')"로 여전히 "맑음"이 같이 잡힘.

### 원인

1단계(substring)와 2단계(임베딩)는 서로 독립적으로 동작함 -
`_semantic_matches()`는 "이미 1단계에서 잡힌 키워드가 포함된 그룹"만
건너뛰고, 그 외의 모든 개념 그룹은 여전히 전체 문장 임베딩과 비교함.
그래서 "바람"/"시원" 그룹은 1단계에서 잡혀서 2단계 비교 대상에서
제외됐지만, "맑음" 그룹은 자기 키워드가 안 걸렸으니 그대로 비교 대상에
남아있었고, 이전과 같은 이유(모델이 "바람 부는 날씨"와 "맑은 날씨"를
비슷한 "날씨 얘기"로 뭉뚱그림)로 threshold를 넘겨버림.

즉 새 키워드를 추가한 건 "바람/시원이 정확히 잡히게" 하는 데는
성공했지만, "맑음이 잘못 같이 잡히는 것"은 별개 문제라 그걸로 해결이
안 됐음 - 두 그룹이 서로 독립적으로 판단되기 때문.

### 대응

기존 `calibrate_embedding_threshold.py`의 TEST_CASES는 "이 문장이 아무
개념과도 안 걸려야 하는지"만 검증해서 이런 "정답 개념은 맞게 잡히는데
동시에 다른 개념과도 겹쳐 걸리는" 교차 오매칭 케이스를 애초에 검출할
수 없는 구조였음. `CROSS_CONTAMINATION_CASES`를 추가해서 "문장 -> 정답
개념 -> 걸리면 안 되는 다른 개념"을 짝지어 그 특정 개념과의 유사도를
직접 출력하도록 스크립트를 보강함(코드 자체는 이 환경에 sentence-transformers가
없어 실행 못 해봄 - `python3 -m py_compile`로 문법만 확인).

### 다음 단계 (로컬 실행 필요)

`python3 calibrate_embedding_threshold.py`를 실제 환경에서 실행해서
"교차 오매칭 체크" 결과를 보고, threshold를 올릴지(다른 매칭 재현율과
trade-off 있음) / "맑음" 예문을 더 구체적으로 다듬을지 / 이 정도는
허용 가능한 한계로 두고 넘어갈지 실측 결과를 보고 판단할 것.
